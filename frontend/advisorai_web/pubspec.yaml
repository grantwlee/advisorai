name: advisorai_web
description: AdvisorAI Web Frontend - Academic advising assistant

# Prevent accidental publishing to pub.dev
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP client for API requests
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Linting rules
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Uncomment to add assets
  # assets:
  #   - images/
  #   - assets/

  # Uncomment to add custom fonts
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf